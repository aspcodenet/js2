name: Build and Deploy
permissions:
  contents: write
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    concurrency: ci-${{ github.ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '16.13.2'

      - name: Install and Build 🔧
        run: | 
          npm ci
          npm run build        
      - name: Deploy 
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: main #The branch the action should deploy to.
          repository-name: aspcodenet/MyQuizSite
          token: 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
          folder: .
